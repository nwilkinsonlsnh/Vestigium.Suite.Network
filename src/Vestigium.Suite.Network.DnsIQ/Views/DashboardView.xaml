<UserControl x:Class="Vestigium.Suite.Network.DnsIQ.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:uc="clr-namespace:Vestigium.Controls.UnderConstruction;assembly=Vestigium.Controls.UnderConstruction"
             Background="{DynamicResource Vestigium.Brushes.Surface.Window}">
  <uc:VestigiumUnderConstruction Title="{Binding Title, Mode=OneWay}"
                                 Subject="{Binding Subject, Mode=OneWay}"
                                 Description="{Binding Description, Mode=OneWay}"
                                 Background="{DynamicResource Vestigium.Brushes.Surface.Window}"
                                 Foreground="{DynamicResource Vestigium.Brushes.Text.Primary}" />
</UserControl>
