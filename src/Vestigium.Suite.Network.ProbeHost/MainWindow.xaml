<Window x:Class="Vestigium.Suite.Network.ProbeHost.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ProbeHost" Height="420" Width="720">
  <DockPanel Margin="16">
    <TextBlock DockPanel.Dock="Top" FontSize="20" FontWeight="SemiBold" Text="ProbeHost" Margin="0,0,0,12" />
    <DockPanel>
      <Button DockPanel.Dock="Top" Content="Snapshot" Command="{Binding RefreshCommand}" HorizontalAlignment="Left" Padding="16,4" Margin="0,0,0,8" />
      <TextBox Text="{Binding Log}" IsReadOnly="True" VerticalScrollBarVisibility="Auto" FontFamily="Consolas" />
    </DockPanel>
  </DockPanel>
</Window>
